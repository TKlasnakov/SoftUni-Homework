/**
 * Created by TKlas on 12.1.2018 г..
 */
function solve(x1, y1, x2, y2) {
    let output = Math.sqrt(Math.pow(x2-x1,2) + Math.pow(y2-y1, 2));
    console.log(output)
}

solve(2.34, 15.66, -13.55, -2.9985);